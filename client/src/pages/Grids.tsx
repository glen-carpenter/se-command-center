import { Grid2x2 } from 'lucide-react'
import PageShell from '../components/PageShell'

export default function Grids() {
  return (
    <PageShell
      title="Grids"
      subtitle="Static and dynamic grids across all sectors"
      icon={<Grid2x2 size={28} />}
      pageId="grids"
    />
  )
}
